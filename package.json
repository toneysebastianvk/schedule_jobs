{
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "node index.js",
    "build": "echo 'Build triggered at ' $(date) && next build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "express": "^5.1.0",
    "vercel": "^48.1.6"
  }
}
